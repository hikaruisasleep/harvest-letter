// --- Atur Posisi ---
var xx = 120; 
var yy = 30;  

// --- Gambar dengan Ukuran Custom (draw_sprite_ext) ---
// Format: sprite, subimg, x, y, xscale, yscale, rot, color, alpha

// Ubah 0.5 di bawah ini sesuai keinginan (0.5 = setengah ukuran)
draw_sprite_ext(sprite_index, image_index, xx, yy, 0.5, 0.5, 0, c_white, 1);